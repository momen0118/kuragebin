# 引き継ぎ（フェーズ1・2・3-1・3-2 → 3-3）

仕様の正は `SPEC.md`、進め方は `CLAUDE.md`。ここはこれまでに何を決めたか、なぜそうしたかのメモ。

## 確認のしかた

- 本番：https://momen0118.github.io/kuragebin/ （main への push で GitHub Actions が公開。作業ブランチは PR にしてユーザーがマージする。フェーズ1・2は momen0118/kuragebin#1 でマージ済み）
- プレビュー：https://claude.ai/artifact/HBagJXCZ9voVBG58Dx3zVR （`#debug` を付けるとデバッグパネル。クエリは渡せない）
  - 作り方：`npx vite build --base ./ --outDir dist-preview`。ページは小さな HTML で、`./assets/index-*.js` と `./assets/index-*.css` を読み、`bg/*.webp` は公開済み
  - 更新：Artifact を `read` してから、`url` を指定して publish する。`files` で新しい js/css を足し、古いものは `null` で消す
  - プレビューでは PWA・オフライン・保存は試せない（Service Worker は動かず、IndexedDB も残らないことがある）
- デバッグ：`?debug`（または `#debug`）でパネル。ほかに `?view=bg|contents|glow|room`（途中の画像）、`?hide=snow,table,...`（部品を隠す）、`?pattern`（屈折を見るための縦縞）
- 撮影：`?debug&capture` だと自動で進まず、`window.__kurage` から `frame(dt)`・`simulate(秒)`・`setHour(h)`・`setLamp(on)`・`setPhoto(only, overlay)`・`tap(x, y)`（画面の CSS px。札かつつく）・`jelly()` を呼べる
  - 3つの瓶（3-2）：`view(v)`（瓶の並びの位置で止める。0 が1番の瓶、途中は小数）・`settle()`（近いほうへ落ち着く）・`jarIndex()`・`spawnIn(瓶, 段階, 進み)`・`fillIn(瓶, n)`・`tagged()`（札を出している個体）
  - 指の操作の途中を撮るときは `play(秒)`：瓶の並び・札も含めて動かし、最後に1回だけ描く。`frame` を何十回も呼ぶと、SwiftShader では描画が溜まってスクショが30秒を越えて失敗する
  - 指の操作そのものは Playwright の `page.mouse`（pointer イベントになる）。長押しと端での待ちは実時間（`setTimeout`）なので `waitForTimeout` で待つ
  - 個体（3-1）：`spawn(段階, 進み, 瓶底の場所)`（番号を返す）・`setStage`・`setProgress`・`setDiscs`・`remove`・`fill(n)`・`clear()`・`state()`・`place(番号, 位置, 上向き)`・`where(番号)`（画面上の位置）・`realSize(on)`
  - 時間：`advance(秒)`（時計を進めて、閉じていた分と同じように進める）
  - `?maxdpr=4` でデバイスピクセル比の上限を上げられる（細部の撮影用。Playwright の deviceScaleFactor も 4 に）
  - 動きはコマ送りの表（数十コマを切り出して並べる）にして確かめた。早送りの一巡は「1時間ずつ進めて10秒動かす」をくり返すと、実際に近い散らばり方になる
  - Playwright は scratchpad に `playwright-core` を入れて使う
  - `chromium.launch({ executablePath: '/opt/pw-browsers/chromium', args: ['--use-angle=swiftshader', '--enable-unsafe-swiftshader', '--ignore-gpu-blocklist'] })`、390×844・DPR 2・`timezoneId: 'Asia/Tokyo'`
  - `__kurageReady` を待ってから `setHour` し、`simulate(2)` で落ち着かせてから撮る
  - dev サーバーはよく落ちるので `(setsid nohup npx vite --port 5173 --host 127.0.0.1 > log 2>&1 < /dev/null &)` で立てる
  - `pkill -f` を使うときは、同じコマンド行にその文字列が入っていると自分のシェルまで殺すので、別の呼び出しにする

## 構成

- `src/render/`：描画
  - `app.ts` が描く順番をまとめる：部屋 → 天板に重ねるもの → 瓶の中身 →（光る種がいるときだけ）発光とブルーム → 合成 → 手前のガラス
  - 部品：`room.ts`（写真の合成）、`table.ts`（影・光の弧）、`jar.ts`／`jarShaders.ts`（ガラスのレンズ・瓶底・水面）、`particles.ts`（マリンスノー・泡）、`jelly/*`（傘・拍動・泳ぎ・触手・口腕）
  - 光：`lighting.ts`（時刻 → 光の状態）、`lamp.ts`（デスクライトの GLSL）
- `src/sim/`：描画に依存しない純粋な TS
  - `state.ts`：状態の型と初期状態
  - `advance.ts`：`advance(state, 秒)` と `catchUp`
  - `lifecycle.ts`：生活環（1刻みごと）、上限と休眠、日誌の出来事
  - `edit.ts`：デバッグパネルからの書き換え（個体を出す・段階・進み・消す・成体で満たす）
  - その他：`clock.ts`（早送り）、`rng.ts`（mulberry32）、`sun.ts`（日の出・日の入り）
- `src/render/creatures.ts`：表示中の瓶の個体を状態に合わせて作る・消す・動かす。奥から描く順番、皿が離れるときの受け渡し
- `src/render/jelly/`：`jellyfish.ts`（泳ぐ個体。育ち具合で成体へ）、`form.ts`（育ち具合 → 形・拍動の調子）、`polyp.ts`／`polypShape.ts`（ポリプとストロビラ）
- `src/storage/`：保存
  - `db.ts`：IndexedDB。使えない環境ではメモリに持つだけにする
  - `schema.ts`：版とマイグレーション
  - `io.ts`：書き出し・読み込み
- `src/ui/`：画面の操作と小さな UI
  - `gestures.ts`：指の操作をタップ・横のスワイプ・長押しに分ける（指1本だけ。縦の動きは何もしない）
  - `jarSlider.ts`：瓶の並びの上の見ている位置。ばねで落ち着く、端の手応え、隣をのぞく・寄る（テストあり）
  - `carry.ts`：つまんで運び、端で隣の瓶へ移す。`tag.ts`：札と名付け。`dots.ts`：下の点3つ。`lampToggle.ts`：ライト
- `src/sim/actions.ts`：利用者の操作による変更（隣の瓶へ移す、名前）
- `src/game.ts`：状態を持ち、進めて保存する
- `src/main.ts`：全体の配線
- `src/pwa/`：`register.ts` が登録する。`sw.js` は Service Worker の本体で、ビルド時に `scripts/pwa-plugin.mjs` がファイル一覧と版を差し込む
- `scripts/convert-images.mjs`：`art/` の元画像から、背景の WebP とアイコンを作る

## フェーズ1で決めたこと（理由）

**部屋**

- カメラ：写真から解いた（焦点距離・地平線・瓶の接地点・瓶の高さ）。3Dの瓶が写真の天板に正しく乗るようにするため
- 写真の置き方：いつも高さいっぱいに置き、縦長の画面では左右を切る。横長の画面では、左右の端をぼかしながら黒に溶かす
- 写真の下端：天板の手前の縁と、その下の暗い帯は見せない。天板のいちばん下の帯を下へ引き伸ばし、強くぼかして黒に溶かす（最後の修正。ホーム画面で黒い帯に見えたため）
- 写真ごとの位置合わせ：4枚は微妙にずれているので、`PHOTO_ALIGN` でそろえた（デバッグパネルで重ねて合わせた）
- ぼかし：壁は強く、瓶の接地する天板は弱く（`ROOM.blurCurve`）。瓶が浮いて見えないように

**光**

- 日の出・日の入り：日本の中央付近（緯度36・経度138、位置情報は使わない）で、その日の値を計算する。光の移り変わりはそこからのずれで置く
- 明け方：写真がないので、夜と昼を重ねて青みを乗せる
- 瓶の影と天板の光の弧：窓から日が直接差す間だけ出す（`DIRECT_SUN`）。以前は日の出前の4:55に弧が出ていた

**瓶と水**

- 瓶：水の入った円筒のレンズとして描く。中央は拡大、縁では像が左右反転して詰まり、縦の明暗の帯になる
  - 画面の外を映すときは写真全体（`roomWideRT`）を読み、写真の外は左が窓の光、右が闇
  - 白い輪郭線は使わず、屈折とフレネルだけで縁を出す
- 天板の光の弧：形を描き足すと「シールみたい」と言われたので、写真の木目そのものを明るくする方式にした
  - 瓶の足もとの淡い光から伸びる
  - 明るさは「暗い」と言われて `arcGain 4.0` まで上げた
  - いつも数秒周期でゆっくり揺らぐ。拍動で水面が揺れると少し強まる。瓶底の光の輪にも弱く同じ揺れ
- マリンスノー：
  - 大きさ：ほとんどはかろうじて見える細かい粒で、大きい粒はまれ
  - 見え方：瓶の軸にピントを合わせて手前と奥はぼかし、奥ほど淡く。自分では光らない
  - 光：昼は窓の側がわずかに明るい。夜はライトの円錐の中だけが見える
  - 「星みたい」と言われて作り直した

**海月**

- 傘：8枚の縁弁ごとに断面を持つ（DataTexture）
  - 形の幅：緩むと皿、縮むとお椀、ときどき強く縮んで釣鐘
  - 縁弁の動き：それぞれしなり、ゆっくり揺れ、ときどき内側へ折れる。縁にはさざ波
- 泳ぎ：推進は縮みの深さと切り離した（`SWIM.thrust 0.1`、`drag 4.0`）。深く縮むたびに勢いよく泳いでしまったため
  - ときどき大きく傾く。多くは手前へ傾き、四つ葉が見える
- 縁触手：張りはゼロ。張りを入れたら「針金になっちゃった」と言われた
  - 重力・水の抵抗・縮むときの噴流・緩むときの吸い込みだけで動く
  - 水の流れは場所で決まるので、近くの触手が一緒に揺れてまとまる
- 発光：ミズクラゲは光らない（`glowStrength`・`pokeGlow` は 0）
  - 仕組みは残してあり、光る種（オワンクラゲなど）を足すときに種ごとに持たせる
  - 光る種がいないときは、発光パスとブルームを飛ばす
- つつき：瓶の空いたところをタップすると、近くの海月が縮んで離れる。海月の上のタップは札（フェーズ3）のために空けてある

**夜のデスクライト**

- 画面外の左上にあるクランプ式のライト。狭い円錐で、天板には右寄りの楕円の光だまりができる。光だまりは昼の写真の木目をライトの色で照らしたもの
- 色：青みのある白 `[0.72, 0.85, 1.0]`。暖色だと水が茶色く濁って見えた。最終的に「もう少し青く、ただし水槽みたいな青にはしない」で決まった
- 点いている時間：日の入り30分後から、日の出60分前まで。明け方は暗くてよい、と確認済み
- アイコン：夜だけ右下に出す。オン・オフは保存する
- 天板の光の弧：ライトは位置が高く、弧が天板まで届かない（物理的に正しい）。夜は影だけで了承済み

**アイコンの配置**

- 右下：瓶に何かをする操作。ライト、あとで餌をいちばん隅に置く
- 左下：日誌
- 下端の中央：瓶の点3つ

## フェーズ2で決めたこと

- 状態：1つのレコードで IndexedDB に保存する（スキーマ版 1）
  - 古い版は `MIGRATIONS` で1版ずつ直す。設定の足りない項目は初期値で埋める
  - 読めないデータは消さずに `broken-<時刻>` に取っておく
  - 新しい版のデータなら上書きしない（読み取り専用で動く）
- 時間：
  - `advance` は60秒の固定刻みで進め、端数は `pending` に持ち越す。刻みが5万を越えるときは刻みを粗くする
  - 乱数の状態も状態に入れる。何回に分けて進めても同じ結果になる（テストあり）
  - 閉じていた分は30日で打ち切る。時計の巻き戻しは経過0として、`lastTick` だけ今に直す
- 保存のタイミング：設定を変えたとき、開いている間は1分ごと、非表示になるとき、`pagehide`
- 早送り：デバッグの早送りと先送りは、ゲームの時間と光の時刻だけにかかる。海月の動きは速くならない（了承済み）
- 書き出し・読み込み：今はデバッグパネルにある。フェーズ3で、日誌の奥の設定へ移す（了承済み）
- Service Worker：全ファイルを事前にキャッシュして、キャッシュから返す。画面を開くときは `?` 付きでも index.html を返す
  - `skipWaiting` はしない。新しい版は次に起動したときから（Playwright で確認済み）
- アイコン：
  - 普通のアイコン：瓶を中心に1.25倍に寄せた（「瓶が大きく見えるように」）
  - maskable：元画像の余白をそのまま使う。絵の半径は35.5%で、安全域の40%に収まる
- iOS：
  - 上端の時計の帯の裏まで描く（`black-translucent`、了承済み）
  - ホーム画面から開くと、ページの高さが時計の帯の分だけ短く報告される（iPhone 15/16 Pro で 852 のところ 793）。
    そのままでは画面の下に帯の高さの黒い帯が出る
  - 対策1（`viewportSize`）：画面の大きさも見て、描く範囲を画面いっぱいにした。
    ただし実機では、描く範囲が伸びてもページの高さより下は表示されなかった（スクショで確認）
  - 対策2（`layout`）：ページそのもの（html と body）の高さも画面いっぱいに伸ばした。
    スクロールでずれないよう、ずれたら戻す。これで iPhone のホーム画面でも画面の下まで描けた（ユーザーの実機で確認済み）
  - 画面の大きさは、1秒ごとに確かめ直す

## フェーズ3-1で決めたこと

**生活環（sim）**

- 成体1匹ごとに約1日（0.8〜1.2日）で瓶底にポリプを1つ付ける。瓶底（ポリプ＋ストロビラ）が3つなら付けない
- ポリプは2〜3日でくびれ始め、ストロビラは半日〜1日でエフィラを放して、ポリプに戻る（実物どおり。ユーザー確認済み）
- 放す数は1〜3匹。くびれ始めるときに、泳ぐ個体の空きの分までに減らす
- 泳ぐ個体（成体＋エフィラ＋ストロビラが放す予定の数）が上限なら、ポリプは休んで進まない。上限は5（6だと瓶の下のほうが不自然に空いて見えた）
- エフィラは5〜7日で成体。段階の中の進みは `progress`（0〜1）で、描画はこれで形を変える
- 日誌の出来事は形のあるデータで記録する（ポリプが付いた・くびれ始めた・離れた・成体になった・休みに入った・動き出した）。文にするのは3-4。端末の時刻は `advance` の中で `lastTick` から割り出す
- 保存の版は2。版1（成体1匹）からそのまま引き継ぐ

**見た目**

- 大きさ：ポリプとエフィラは実物のおよそ2倍（ポリプの体の高さ 0.0192、放されたエフィラの半径 0.016）。2.5倍は大きすぎると言われた。ポリプは「ふと見ると何か生えている」くらい
- エフィラ：8本の腕の星形。腕は先へ少し細り、先は二股。育ち具合で腕の間が埋まり、縁触手（腕の間から）、口腕、最後に四つ葉。g = 1 でちょうど成体の値になる（成体の見た目の値は変えていない）
- 拍動はぎこちなく速い（間が空く・続けて縮む・腕ごとにずれる）。転がりは控えめ（「いつも横倒し」にならない程度）
- 生まれたてのエフィラは成体より少し濃い（透けすぎて見えないので）
- ストロビラ：皿の数＝放すエフィラの数（最大3枚）。皿の幅は放されたエフィラに合わせてポリプより広い。終わりごろ皿がぴくぴくし、上から1枚ずつ、腕を畳んだエフィラになって浮き上がる
- ポリプ：つつくと縮んで数秒で開く。休んでいる間は触手を縮めて丸め、揺れを遅く
- 付いたばかりのポリプは小さく、半日で育つ。エフィラを放したあとは根元から生え直す（8時間）

**泳ぐ個体どうし**

- 画面の上で重ならないように、奥行きの差は 0.35 倍に数えて、横と上下に離れる（`SWIM.othersMargin` 1.2、`othersPush` 0.06）
- 測った値（10分泳がせて）：6匹で「2匹が半分以上重なる」時間 75% → 1.5%、ほぼ重なるのは 0%。4匹はどちらも 0%
- 新しく現れる個体は、ほかの個体から離れた所に置く
- 高さの分布：ひとりのときは水の真ん中あたりを漂う（フェーズ1のまま）。ほかの個体がいるときは、沈んでいく先を底の近くまで広げる（`SWIM.bottomPadding` 0.16、ひとりのとき用の `driftPadding` 0.2）。
  5匹で、水を下から6段に分けた2〜5段目にほぼ均等（それまでは下の段が少なく、上に押し出されていた）

**性能**

- 泳ぐ個体6匹の動きの計算は、こちらの環境で1フレーム約2.3ms（触手の計算を無駄なく直して 3.3ms から）。実機の fps は未確認

## フェーズ3-2で決めたこと

**3つの瓶の並び（案A＋視差、ユーザー指定）**

- 3つの瓶は天板の上に横に並んでいる。スワイプすると見ている側が机に沿って横へ一歩ずれたように見える
- 瓶 i は x = (i − 見ている位置) × 間隔 に立つ。間隔は、止まっているときに隣の瓶が画面の外にちょうど隠れる幅に `SWIPE.gap` を足したもの
- 描くのは見えている瓶だけ（ふだん1つ、すれ違う間は2つ）。瓶ごとにカメラを横へずらして、瓶は原点のまま描く。デスクライトも瓶ごとにあり、瓶と一緒に動く
- 部屋の写真は同じ向きにほんの少しだけずらす（視差）。奥の壁ほど小さく（`parallaxWall` 0.06）、手前の天板ほど大きく（`parallaxTable` 0.2）、写真の y で切り替える
  - 1番の瓶で写真の元の位置。3番の瓶（と端で引っ張った分）まで、写真の端が見えない範囲に収める
- すれ違う間は、2つの瓶の影・光の弧・夜の光だまり・天板への映り込みが重なって見える
- 横へ離れた瓶の縁は、写真の外の左を窓の光にしない（写真を折り返して読む）。そのままだと、左へ抜けていく瓶の右の縁が白い板のように光った。見ている瓶の見た目は変えていない（`SWIPE.windowSideFade`）

**スワイプ**

- 横に10px 動いたらスワイプ（縦のほうが大きければ何もしない）。指についていき、離すと近いほうの瓶へばねで落ち着く
- 速く払ったら（1.2瓶/秒を越えたら）払った向きの隣の瓶へ。1回で動くのは隣の瓶まで。3と1はつながっていない
- 端の瓶の外へは少しだけ引っ張れる（上限 0.12瓶）。動いている途中で触ると、その場でつかんで止まる
- 落ち着いた瓶を `settings.jar` に保存して、次に開いたときはその瓶から（ユーザーの反対なし）
- 下端の中央の点3つ：今の瓶ほど明るい（0.2〜0.55）。スワイプの途中はなめらかに移る。右下のライトのアイコンと高さをそろえた

**札（海月をタップ）**

- 泳ぐ個体も瓶底のポリプ・ストロビラも出る。個体のすぐ上に、名前・段階・「この瓶で◯日目」（来た日が1日目）。4.5秒で消える
- 小さな個体は指の大きさ（半径22px）まで広げて拾う。何もないところのタップは今までどおりつつく
- 名前（初めは「名無し」、薄く）をタップすると入力欄になる。Enter か欄の外をタップで決まる、Esc でやめる。12文字まで、空なら名無しに戻す
  - かな漢字変換の確定の Enter では閉じない
  - 入力中はキーボードに隠れないよう、札を画面の上から34%より上へ動かす。文字は16px（iOS が拡大しないように）
- スワイプや長押しを始めると、札はすぐ消える

**隣の瓶へ移す（長押し）**

- 動かせるのは泳ぐ個体（成体・エフィラ）だけ（ユーザー確認済み）。ポリプとストロビラは瓶底に付いているので動かさない
- 泳ぐ個体の上で0.45秒押すと、つまむ（きゅっと少し縮む）。指の先へ水の中を少し遅れて引かれていく（ばね）。瓶の外へは出ない
- 画面の左右の端（幅の12%）へ運ぶと、隣の瓶のほうへ少し寄り、0.3秒置くと移す
  - 移せる：描画を先に隣の瓶へ渡してから状態を変え、見ている瓶も隣へ動く。移ってきた個体は、来た側の水面の近くから注ぎ入れたようにゆっくり沈んでくる
  - 隣が上限：隣の瓶を0.4瓶ぶんのぞいて（混んでいるのが見える）戻り、個体は瓶の真ん中のほうへ引き戻されてから放される（0.25だと隣の瓶の縁しか見えなかった）
  - 端の瓶の外側：少し引っ張れて戻り、個体は真ん中のほうへ
- 1回の長押しで試すのは1回だけ。移した直後の成体は「次のポリプまで」を0から数えなおす（移し先の瓶底にすぐポリプが付かないように）
- 移したことは日誌には書かない（日誌の出来事の種類にない）

## config.ts の主な値

- `PHOTO`、`PHOTO_ALIGN`：写真の寸法、写真から解くカメラの手がかり、写真ごとのずれ
- `ROOM`：写真の合成
  - `blurCurve`：写真の y に応じたぼかし
  - `edgeFade`：横長の画面で左右を溶かす幅
  - `foreground*`：手前の天板の引き伸ばし・ぼけ・暗さ。縁は y=1354〜1370 なので、`foregroundStart + foregroundBand` はそれより上に収める
- `JAR`：瓶の形、屈折率、レンズの見え方
  - `lensBackdrop*`：像がどれだけ拡大・反転するか
  - `waterTint`、`glassTint`：水とガラスの色
  - ハイライトの強さ
- `TABLE`、`CAUSTIC`：影と光の弧、揺らぎ
- `WATER`：マリンスノー・泡・水面の揺れ
- 海月：
  - `PULSE`：拍動。縮み0.3秒・緩み1.2秒、深さのばらつき、強い拍動の確率
  - `BELL`：傘の形と縁弁
  - `SWIM`：泳ぎ・傾き・壁のよけ方
  - `POKE`：つつき
  - `TENTACLES`、`ORAL_ARMS`：縁触手と口腕
  - `JELLY_LOOK`：色、発光、ライトの散り
- 光：
  - `LIGHT_LOOKS`、`LIGHT_SCHEDULE`：時刻ごとの光と、その並び
  - `SUN`：日の出・日の入りを計算する場所
  - `DIRECT_SUN`：直射の出入り
  - `LAMP`：デスクライト
  - `RIM_WARM`：夕方、瓶の縁に一瞬乗る温度
- `SIM`：固定刻み60秒、上限30日、保存間隔60秒、瓶3つ、世界のシード
- `LIFE`：生活環の期間、放す数、上限（泳ぐ個体5・ポリプ3）、ポリプの付く場所。`JOURNAL`：日誌の数の上限
- `EPHYRA`：エフィラの大きさ・腕の形・育ち具合ごとの変化・ぎこちない拍動・泳ぎ。`POLYP`：ポリプの形・触手・揺れ・つつき・休み・育ち。`STROBILA`：伸び・くびれ・皿・ぴくぴく・離れる間隔
- `SWIPE`：瓶の間隔の足し分、写真の視差、スワイプの判定・払う速さ・ばね・端の手応え
- `HANDLING`：指で押せる大きさ、長押しの時間、つまんだときの縮み、端の幅と待ち、寄る量、隣をのぞく量、戻す時間
- `TAG`：札の出ている時間・消える時間・個体との間・入力中の高さ。`DOTS`：点の明るさ
- `DEBUG`：早送りの倍率と、ボタンで進める時間
- `RENDER`：DPR の上限2、ブルーム、起動時に暗転から明ける時間。`seed` は今の1匹の見た目と動きの種

見た目の値はどれもユーザーと何往復もして決めたもの。黙って変えない。

## 既知の問題・後回し

- 実機の性能（中級スマホで60fps）は未確認。こちらでは SwiftShader でしか見ていない。画質3段階はフェーズ4
- iOS のホーム画面での下の黒い帯は、対策2で解決済み（上の「iOS」）。iOS の更新でまた出るようなら、案は2つで、どちらにするかはユーザーに訊く
  - 案A：時計の帯の裏に描くのをやめる（`black` にする）。時計の帯は黒くなり、部屋の上端はそこへ溶かす
  - 案B：今のまま、天板が下の黒い帯へ途切れずに溶けるようにする
- 桜の写真は読み込んでいるが使っていない（重みはいつも0）。桜の季節はフェーズ4
- 早送り（×3600）では、放されたエフィラが散らばる前に育つので、しばらく底のほうに固まって見える（実時間なら数分で散らばる）
- 泳ぎのテストは数分ぶん動きを回すので遅い（全体で約45秒）
- Android のスプラッシュ画面：アイコンの黒（ほぼ #000）と背景色 #111 の差が見えるかもしれない（未確認）

## 次にやること（3-3 から）

3-1（生活環）は済んだ。3-2（3つの瓶）は実装して PR にした（ユーザーの確認待ち。直しがあればそこから）。残りは3-3〜3-5（計画はユーザーと合意済み）。

- 3-3 餌：右下の隅（ライトはその内側）、胃の橙色が数時間で薄れる、その間だけ段階の進みが少し速い（最大2割）、1瓶数時間に1回
- 3-4 観察日誌：左下、未読の点、日付ごとの記録・個体・設定（書き出し・読み込みを移す）
- 3-5 瓶を揺らす：SPEC の節どおり
- 各段階の始めに、その段階の「先に決めたいこと」をユーザーに確かめる
