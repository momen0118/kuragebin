# 引き継ぎ（フェーズ1・2 → フェーズ3）

仕様の正は `SPEC.md`、進め方は `CLAUDE.md`。ここはフェーズ1・2で何を決めたか、なぜそうしたかのメモ。

## 確認のしかた

- 本番：https://momen0118.github.io/kuragebin/ （main への push で GitHub Actions が公開。作業ブランチは PR にしてユーザーがマージする。フェーズ1・2は momen0118/kuragebin#1 でマージ済み）
- プレビュー：https://claude.ai/artifact/HBagJXCZ9voVBG58Dx3zVR （`#debug` を付けるとデバッグパネル。クエリは渡せない）
  - 作り方：`npx vite build --base ./ --outDir dist-preview`。ページは小さな HTML で、`./assets/index-*.js` と `./assets/index-*.css` を読み、`bg/*.webp` は公開済み
  - 更新：Artifact を `read` してから、`url` を指定して publish する。`files` で新しい js/css を足し、古いものは `null` で消す
  - プレビューでは PWA・オフライン・保存は試せない（Service Worker は動かず、IndexedDB も残らないことがある）
- デバッグ：`?debug`（または `#debug`）でパネル。ほかに `?view=bg|contents|glow|room`（途中の画像）、`?hide=snow,table,...`（部品を隠す）、`?pattern`（屈折を見るための縦縞）
- 撮影：`?debug&capture` だと自動で進まず、`window.__kurage` から `frame(dt)`・`simulate(秒)`・`setHour(h)`・`setLamp(on)`・`setPhoto(only, overlay)`・`tap(x, y)`・`jelly()` を呼べる
  - Playwright は scratchpad に `playwright-core` を入れて使う
  - `chromium.launch({ executablePath: '/opt/pw-browsers/chromium', args: ['--use-angle=swiftshader', '--enable-unsafe-swiftshader', '--ignore-gpu-blocklist'] })`、390×844・DPR 2・`timezoneId: 'Asia/Tokyo'`
  - `__kurageReady` を待ってから `setHour` し、`simulate(2)` で落ち着かせてから撮る
  - dev サーバーはよく落ちるので `(setsid nohup npx vite --port 5173 --host 127.0.0.1 > log 2>&1 < /dev/null &)` で立てる
  - `pkill -f` を使うときは、同じコマンド行にその文字列が入っていると自分のシェルまで殺すので、別の呼び出しにする

## 構成

- `src/render/`：描画
  - `app.ts` が描く順番をまとめる：部屋 → 天板に重ねるもの → 瓶の中身 →（光る種がいるときだけ）発光とブルーム → 合成 → 手前のガラス
  - 部品：`room.ts`（写真の合成）、`table.ts`（影・光の弧）、`jar.ts`／`jarShaders.ts`（ガラスのレンズ・瓶底・水面）、`particles.ts`（マリンスノー・泡）、`jelly/*`（傘・拍動・泳ぎ・触手・口腕）
  - 光：`lighting.ts`（時刻 → 光の状態）、`lamp.ts`（デスクライトの GLSL）
- `src/sim/`：描画に依存しない純粋な TS
  - `state.ts`：状態の型と初期状態
  - `advance.ts`：`advance(state, 秒)` と `catchUp`
  - その他：`clock.ts`（早送り）、`rng.ts`（mulberry32）、`sun.ts`（日の出・日の入り）
- `src/storage/`：保存
  - `db.ts`：IndexedDB。使えない環境ではメモリに持つだけにする
  - `schema.ts`：版とマイグレーション
  - `io.ts`：書き出し・読み込み
- `src/game.ts`：状態を持ち、進めて保存する
- `src/main.ts`：全体の配線
- `src/pwa/`：`register.ts` が登録する。`sw.js` は Service Worker の本体で、ビルド時に `scripts/pwa-plugin.mjs` がファイル一覧と版を差し込む
- `scripts/convert-images.mjs`：`art/` の元画像から、背景の WebP とアイコンを作る

## フェーズ1で決めたこと（理由）

**部屋**

- カメラ：写真から解いた（焦点距離・地平線・瓶の接地点・瓶の高さ）。3Dの瓶が写真の天板に正しく乗るようにするため
- 写真の置き方：いつも高さいっぱいに置き、縦長の画面では左右を切る。横長の画面では、左右の端をぼかしながら黒に溶かす
- 写真の下端：天板の手前の縁と、その下の暗い帯は見せない。天板のいちばん下の帯を下へ引き伸ばし、強くぼかして黒に溶かす（最後の修正。ホーム画面で黒い帯に見えたため）
- 写真ごとの位置合わせ：4枚は微妙にずれているので、`PHOTO_ALIGN` でそろえた（デバッグパネルで重ねて合わせた）
- ぼかし：壁は強く、瓶の接地する天板は弱く（`ROOM.blurCurve`）。瓶が浮いて見えないように

**光**

- 日の出・日の入り：日本の中央付近（緯度36・経度138、位置情報は使わない）で、その日の値を計算する。光の移り変わりはそこからのずれで置く
- 明け方：写真がないので、夜と昼を重ねて青みを乗せる
- 瓶の影と天板の光の弧：窓から日が直接差す間だけ出す（`DIRECT_SUN`）。以前は日の出前の4:55に弧が出ていた

**瓶と水**

- 瓶：水の入った円筒のレンズとして描く。中央は拡大、縁では像が左右反転して詰まり、縦の明暗の帯になる
  - 画面の外を映すときは写真全体（`roomWideRT`）を読み、写真の外は左が窓の光、右が闇
  - 白い輪郭線は使わず、屈折とフレネルだけで縁を出す
- 天板の光の弧：形を描き足すと「シールみたい」と言われたので、写真の木目そのものを明るくする方式にした
  - 瓶の足もとの淡い光から伸びる
  - 明るさは「暗い」と言われて `arcGain 4.0` まで上げた
  - いつも数秒周期でゆっくり揺らぐ。拍動で水面が揺れると少し強まる。瓶底の光の輪にも弱く同じ揺れ
- マリンスノー：
  - 大きさ：ほとんどはかろうじて見える細かい粒で、大きい粒はまれ
  - 見え方：瓶の軸にピントを合わせて手前と奥はぼかし、奥ほど淡く。自分では光らない
  - 光：昼は窓の側がわずかに明るい。夜はライトの円錐の中だけが見える
  - 「星みたい」と言われて作り直した

**海月**

- 傘：8枚の縁弁ごとに断面を持つ（DataTexture）
  - 形の幅：緩むと皿、縮むとお椀、ときどき強く縮んで釣鐘
  - 縁弁の動き：それぞれしなり、ゆっくり揺れ、ときどき内側へ折れる。縁にはさざ波
- 泳ぎ：推進は縮みの深さと切り離した（`SWIM.thrust 0.1`、`drag 4.0`）。深く縮むたびに勢いよく泳いでしまったため
  - ときどき大きく傾く。多くは手前へ傾き、四つ葉が見える
- 縁触手：張りはゼロ。張りを入れたら「針金になっちゃった」と言われた
  - 重力・水の抵抗・縮むときの噴流・緩むときの吸い込みだけで動く
  - 水の流れは場所で決まるので、近くの触手が一緒に揺れてまとまる
- 発光：ミズクラゲは光らない（`glowStrength`・`pokeGlow` は 0）
  - 仕組みは残してあり、光る種（オワンクラゲなど）を足すときに種ごとに持たせる
  - 光る種がいないときは、発光パスとブルームを飛ばす
- つつき：瓶の空いたところをタップすると、近くの海月が縮んで離れる。海月の上のタップは札（フェーズ3）のために空けてある

**夜のデスクライト**

- 画面外の左上にあるクランプ式のライト。狭い円錐で、天板には右寄りの楕円の光だまりができる。光だまりは昼の写真の木目をライトの色で照らしたもの
- 色：青みのある白 `[0.72, 0.85, 1.0]`。暖色だと水が茶色く濁って見えた。最終的に「もう少し青く、ただし水槽みたいな青にはしない」で決まった
- 点いている時間：日の入り30分後から、日の出60分前まで。明け方は暗くてよい、と確認済み
- アイコン：夜だけ右下に出す。オン・オフは保存する
- 天板の光の弧：ライトは位置が高く、弧が天板まで届かない（物理的に正しい）。夜は影だけで了承済み

**アイコンの配置**

- 右下：瓶に何かをする操作。ライト、あとで餌をいちばん隅に置く
- 左下：日誌
- 下端の中央：瓶の点3つ

## フェーズ2で決めたこと

- 状態：1つのレコードで IndexedDB に保存する（スキーマ版 1）
  - 古い版は `MIGRATIONS` で1版ずつ直す。設定の足りない項目は初期値で埋める
  - 読めないデータは消さずに `broken-<時刻>` に取っておく
  - 新しい版のデータなら上書きしない（読み取り専用で動く）
- 時間：
  - `advance` は60秒の固定刻みで進め、端数は `pending` に持ち越す。刻みが5万を越えるときは刻みを粗くする
  - 乱数の状態も状態に入れる。何回に分けて進めても同じ結果になる（テストあり）
  - 閉じていた分は30日で打ち切る。時計の巻き戻しは経過0として、`lastTick` だけ今に直す
- 保存のタイミング：設定を変えたとき、開いている間は1分ごと、非表示になるとき、`pagehide`
- 早送り：デバッグの早送りと先送りは、ゲームの時間と光の時刻だけにかかる。海月の動きは速くならない（了承済み）
- 書き出し・読み込み：今はデバッグパネルにある。フェーズ3で、日誌の奥の設定へ移す（了承済み）
- Service Worker：全ファイルを事前にキャッシュして、キャッシュから返す。画面を開くときは `?` 付きでも index.html を返す
  - `skipWaiting` はしない。新しい版は次に起動したときから（Playwright で確認済み）
- アイコン：
  - 普通のアイコン：瓶を中心に1.25倍に寄せた（「瓶が大きく見えるように」）
  - maskable：元画像の余白をそのまま使う。絵の半径は35.5%で、安全域の40%に収まる
- iOS：
  - 上端の時計の帯の裏まで描く（`black-translucent`、了承済み）
  - ホーム画面から開くと、ページの高さが時計の帯の分だけ短く報告される（iPhone 15/16 Pro で 852 のところ 793）。
    そのままでは画面の下に帯の高さの黒い帯が出る
  - 対策1（`viewportSize`）：画面の大きさも見て、描く範囲を画面いっぱいにした。
    ただし実機では、描く範囲が伸びてもページの高さより下は表示されなかった（スクショで確認）
  - 対策2（`layout`）：ページそのもの（html と body）の高さも画面いっぱいに伸ばした。
    スクロールでずれないよう、ずれたら戻す。これで iPhone のホーム画面でも画面の下まで描けた（ユーザーの実機で確認済み）
  - 画面の大きさは、1秒ごとに確かめ直す

## config.ts の主な値

- `PHOTO`、`PHOTO_ALIGN`：写真の寸法、写真から解くカメラの手がかり、写真ごとのずれ
- `ROOM`：写真の合成
  - `blurCurve`：写真の y に応じたぼかし
  - `edgeFade`：横長の画面で左右を溶かす幅
  - `foreground*`：手前の天板の引き伸ばし・ぼけ・暗さ。縁は y=1354〜1370 なので、`foregroundStart + foregroundBand` はそれより上に収める
- `JAR`：瓶の形、屈折率、レンズの見え方
  - `lensBackdrop*`：像がどれだけ拡大・反転するか
  - `waterTint`、`glassTint`：水とガラスの色
  - ハイライトの強さ
- `TABLE`、`CAUSTIC`：影と光の弧、揺らぎ
- `WATER`：マリンスノー・泡・水面の揺れ
- 海月：
  - `PULSE`：拍動。縮み0.3秒・緩み1.2秒、深さのばらつき、強い拍動の確率
  - `BELL`：傘の形と縁弁
  - `SWIM`：泳ぎ・傾き・壁のよけ方
  - `POKE`：つつき
  - `TENTACLES`、`ORAL_ARMS`：縁触手と口腕
  - `JELLY_LOOK`：色、発光、ライトの散り
- 光：
  - `LIGHT_LOOKS`、`LIGHT_SCHEDULE`：時刻ごとの光と、その並び
  - `SUN`：日の出・日の入りを計算する場所
  - `DIRECT_SUN`：直射の出入り
  - `LAMP`：デスクライト
  - `RIM_WARM`：夕方、瓶の縁に一瞬乗る温度
- `SIM`：固定刻み60秒、上限30日、保存間隔60秒、瓶3つ、世界のシード
- `DEBUG`：早送りの倍率と、ボタンで進める時間
- `RENDER`：DPR の上限2、ブルーム、起動時に暗転から明ける時間。`seed` は今の1匹の見た目と動きの種

見た目の値はどれもユーザーと何往復もして決めたもの。黙って変えない。

## 既知の問題・後回し

- 実機の性能（中級スマホで60fps）は未確認。こちらでは SwiftShader でしか見ていない。画質3段階はフェーズ4
- iOS のホーム画面での下の黒い帯は、対策2で解決済み（上の「iOS」）。iOS の更新でまた出るようなら、案は2つで、どちらにするかはユーザーに訊く
  - 案A：時計の帯の裏に描くのをやめる（`black` にする）。時計の帯は黒くなり、部屋の上端はそこへ溶かす
  - 案B：今のまま、天板が下の黒い帯へ途切れずに溶けるようにする
- 桜の写真は読み込んでいるが使っていない（重みはいつも0）。桜の季節はフェーズ4
- 描いている海月は1匹だけで、見た目と動きの種は `RENDER.seed` で固定。状態の個体（`Creature.seed`）とはまだつないでいない
- `sim/advance.ts` の `step` は時間と年齢を進めるだけ。生活環はフェーズ3
- 泳ぎのテストは数分ぶん動きを回すので遅い（全体で約45秒）
- Android のスプラッシュ画面：アイコンの黒（ほぼ #000）と背景色 #111 の差が見えるかもしれない（未確認）

## フェーズ3で最初にやること

SPEC の「3. 育つ瓶」：生活環（ポリプ・ストロビラ・エフィラ）、3瓶スワイプ、個体の移動、餌、観察日誌、瓶を揺らす。あわせて、海月をタップすると出る札（名前・段階・来てからの日数、名付け）と、日誌の奥の設定（音・揺れ・画質・書き出し/読み込みの移動）も作る。

1. 始める前にユーザーに確認を取る（CLAUDE.md）。仕様にない見た目は先に訊く。たとえば札・日誌の見た目、エフィラとポリプの形、スワイプ中の光の重なり
2. シミュレーションから作る。`step` に生活環を足す
   - 期間と上限は config に置く（1瓶に泳ぐ個体6・ポリプ3、上限に達したら休眠）
   - 日誌の出来事も sim が作る
   - 乱数はシード付きのまま。Vitest で「同じ状態と経過から同じ結果」を確かめる
3. 描画を状態につなぐ。表示中の瓶の個体をすべて描き、他の瓶はシミュレーションだけ。個体の種は `Creature.seed` から取る
4. そのあとに、3瓶スワイプと点、札、餌、個体の移動、日誌と設定、瓶を揺らす（DeviceMotion。デバッグパネルにセンサーなしの操作）を足す
5. 状態の形を変えたら `SCHEMA_VERSION` を上げ、`MIGRATIONS` に足す（すでに保存されているデータがある）
