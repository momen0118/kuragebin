# 海月瓶（仮題）

暗い部屋の低いテーブルに置いた瓶の中で、海月が実時間で勝手に生きている放置ゲーム。
GitHub Pages で公開する PWA。仕様の正は `SPEC.md`。この CLAUDE.md は進め方と守ることだけを書く。

## 進め方

- `SPEC.md` の「実装フェーズ」に沿って、1フェーズずつ作る
- 各フェーズの終わりで必ず止まり、確認を取ってから次へ進む。報告には次を書く
  - 何ができたか（数行）
  - 確認方法（ローカルのURL、または Pages のURL）
  - 見てほしい点、判断を仰ぎたい点
- 仕様にないこと、仕様と食い違う判断が要ることは、黙って決めずに先に訊く
- 仕様にない機能を気を利かせて足さない。特に「やらないこと」（SPEC.md のコンセプト節）は厳守
- 報告は短く、専門用語は必要な分だけ

## 最優先事項

フェーズ1（海月一匹の描画）に一番時間をかける。
成功の基準は「何も操作せずに30秒眺めていられること」。機能より動きと質感を優先する。
拍動の緩急、触手の遅れ、透け感、発光。ここが気持ちよくなるまで次へ進まない。

## 技術スタック

- Vite + TypeScript（strict）
- three.js ＋ 自作シェーダ（WebGL2）。Canvas 2D だけで描かない
- 保存は IndexedDB
- テストは Vitest（シミュレーション部分）
- GitHub Actions で main への push ごとに Pages へデプロイ。`vite.config.ts` の `base` をリポジトリ名に合わせる
- PWA：manifest、Service Worker による全アセットの事前キャッシュ、完全オフライン動作

## 構成の方針

```
src/
  config.ts        調整用の数値はすべてここ（成長期間、上限、確率、拍動の速さなど）
  sim/             シミュレーション。描画・DOM・three.js に依存しない純粋なTS
  render/          three.js とシェーダ
  ui/              日誌、札、設定などの最小限のUI
  storage/         IndexedDB、スキーマ番号、マイグレーション、書き出し・読み込み
  debug/           デバッグパネル
public/
  bg/              背景写真
  icons/           PWAアイコン
```

- シミュレーションは `advance(state, 経過秒)` の純関数。起動時・復帰時に経過分を固定刻みで進める
- 乱数はシード付き。同じ状態と経過から同じ結果になること
- 経過は最大30日で打ち切り。端末時計の巻き戻しは経過0扱い
- マジックナンバーを散らさない。数値は `config.ts` へ

## 画像

リポジトリ直下に置いてあるものを `public/` 以下へ移して使う。

| ファイル | 用途 |
| --- | --- |
| `bg-day.png` | 背景・昼 |
| `bg-sakura.png` | 背景・桜の季節の昼（枝の影入り） |
| `bg-dusk.png` | 背景・夕方 |
| `bg-night.png` | 背景・夜 |
| `icon-source.png` | アイコン元画像（正方形） |

- 背景は WebP か AVIF、長辺2048px前後に変換
- アイコンは 192 / 512 / maskable を生成。maskable は元画像の余白を活かす
- 明け方の写真はない。夜→昼のクロスフェード中に青みを弱く乗せて作る
- 桜の桃色は写真に焼き込まれていない。壁の明るい部分だけにゲーム側で重ねる
- ぼかしは一律にしない。壁側は強く、瓶が接地する天板付近は弱く
- 瓶の口は開いたまま、蓋はない（アイコンと揃える）

## デバッグパネル

本番でも隠しで残す（例：URL に `?debug` を付けたときだけ表示）。

- 時刻の上書きスライダー（光の確認用）
- 時間の早送り（×60、×3600 など）と、任意の経過時間を一気に進めるボタン
- 個体の生成、段階の強制変更
- 状態のリセット
- FPS 表示

## 性能

- 中級スマホで60fps目標、デバイスピクセル比は2で頭打ち
- 画質3段階（高・中・低）
- 描画するのは表示中の瓶だけ。他の瓶はシミュレーションのみ
- タブ非表示中は描画を止める

## 対象環境

iOS Safari（ホーム画面追加）と Android Chrome を優先。縦画面固定、セーフエリア対応。
